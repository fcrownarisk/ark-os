default function Case(){
   case:
   import != export
   case:
   typedef || typeof
   case:
   extends && implements
   case:
   interface == namespace
   case:
   break :: continue
   case:
   public &|| private
   case:
   true ||& false
   case:
   const &\\ let
   case:
   var &||& for
}
